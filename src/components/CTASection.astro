---
import { serviceWhatsAppUrl, site } from '@data/site';

interface Props {
  title?: string;
  description?: string;
}

const {
  title = 'Need Professional Help?',
  description = 'Tell us your requirement and we will guide you with the right process, documents, pricing, and timeline.'
} = Astro.props;
---

<section class="bg-navy-900">
  <div class="mx-auto flex max-w-7xl flex-col gap-6 px-4 py-14 text-white sm:px-6 md:flex-row md:items-center md:justify-between lg:px-8">
    <div class="max-w-3xl">
      <h2 class="text-2xl font-bold tracking-normal">{title}</h2>
      <p class="mt-3 text-sm leading-7 text-slate-300">{description}</p>
      <div class="mt-5 grid gap-2 text-sm text-slate-300 sm:grid-cols-3">
        <a href={site.phoneHref} class="transition hover:text-teal-300">Phone: {site.phone}</a>
        <a href={site.serviceEmailHref} class="transition hover:text-teal-300">Email: {site.serviceEmail}</a>
        <span>WhatsApp Support: Available</span>
      </div>
    </div>
    <div class="flex flex-col gap-3 sm:flex-row">
      <a href={site.phoneHref} class="rounded-lg border border-white/30 px-5 py-3 text-center text-sm font-bold text-white transition hover:border-teal-300 hover:bg-white/10">
        Call Now
      </a>
      <a href={serviceWhatsAppUrl} target="_blank" rel="noopener noreferrer" class="rounded-lg bg-teal-500 px-5 py-3 text-center text-sm font-bold text-white shadow-sm transition hover:bg-teal-600">
        Talk on WhatsApp
      </a>
    </div>
  </div>
</section>
