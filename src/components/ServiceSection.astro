---
interface Props {
  title: string;
  body?: string[];
  items?: string[];
}

const { title, body = [], items = [] } = Astro.props;
---

<section class="premium-card p-6">
  <h2 class="text-2xl font-bold tracking-normal text-navy-900">{title}</h2>
  {body.length > 0 && (
    <div class="mt-4 space-y-4 text-sm leading-7 text-slate-600">
      {body.map((paragraph) => <p>{paragraph}</p>)}
    </div>
  )}
  {items.length > 0 && (
    <ul class="mt-5 grid gap-3 text-sm leading-6 text-slate-700 sm:grid-cols-2">
      {items.map((item) => <li class="rounded-lg border border-slate-200 bg-slate-50 p-3 font-semibold transition hover:border-teal-500/40 hover:bg-teal-50/60">{item}</li>)}
    </ul>
  )}
</section>
