---
const serviceOptions = [
  'GST Registration',
  'GST Return Filing',
  'GST Notice Support',
  'Company Registration',
  'LLP Registration',
  'OPC Registration',
  'Partnership Firm Registration',
  'Income Tax Return Filing',
  'FSSAI Registration',
  'Startup India Registration',
  'MSME/Udyam Registration',
  'GeM Registration',
  'IEC Registration',
  'ROC Filing / Annual Compliance',
  'FEMA Compliance',
  'Trademark Registration',
  'Tax Consultation',
  'Other Service'
];

const contactMethods = [
  'Phone Call',
  'WhatsApp',
  'Email'
];
---

<form name="contact" method="POST" data-netlify="true" netlify-honeypot="bot-field" action="/contact" class="grid gap-4 rounded border border-slate-200 bg-white p-6 shadow-soft">
  <input type="hidden" name="form-name" value="contact" />
  <p class="hidden">
    <label>Do not fill this field <input name="bot-field" /></label>
  </p>
  <div>
    <label for="full-name" class="text-sm font-bold text-navy-900">Full Name</label>
    <input id="full-name" name="Full Name" type="text" required autocomplete="name" placeholder="Enter your full name" class="mt-2 w-full rounded border border-slate-300 px-3 py-3 text-sm" />
  </div>
  <div class="grid gap-4 sm:grid-cols-2">
    <div>
      <label for="phone" class="text-sm font-bold text-navy-900">Phone Number</label>
      <input id="phone" name="Phone Number" type="tel" required autocomplete="tel" placeholder="Enter your mobile number" class="mt-2 w-full rounded border border-slate-300 px-3 py-3 text-sm" />
    </div>
    <div>
      <label for="email" class="text-sm font-bold text-navy-900">Email Address</label>
      <input id="email" name="Email Address" type="email" autocomplete="email" placeholder="Enter your email address" class="mt-2 w-full rounded border border-slate-300 px-3 py-3 text-sm" />
    </div>
  </div>
  <div>
    <label for="service" class="text-sm font-bold text-navy-900">Service Required</label>
    <select id="service" name="Service Required" required class="mt-2 w-full rounded border border-slate-300 px-3 py-3 text-sm">
      <option value="">Select a service</option>
      {serviceOptions.map((service) => <option value={service}>{service}</option>)}
    </select>
  </div>
  <div>
    <label for="message" class="text-sm font-bold text-navy-900">Message</label>
    <textarea id="message" name="Message" rows="5" required placeholder="Briefly describe your requirement" class="mt-2 w-full rounded border border-slate-300 px-3 py-3 text-sm"></textarea>
  </div>
  <div>
    <label for="preferred-contact-method" class="text-sm font-bold text-navy-900">Preferred Contact Method</label>
    <select id="preferred-contact-method" name="Preferred Contact Method" required class="mt-2 w-full rounded border border-slate-300 px-3 py-3 text-sm">
      <option value="">Select contact method</option>
      {contactMethods.map((method) => <option value={method}>{method}</option>)}
    </select>
  </div>
  <button type="submit" class="rounded bg-navy-900 px-5 py-3 text-sm font-bold text-white hover:bg-navy-800">
    Submit Inquiry
  </button>
</form>
